/*
void backspace() {
  
  key = keypad.getKey();
  if (key != 'F') return;

  if  (index > 0) {

  index--;
  pad.remove(index, 1); 
  lcd.setCursor(0, 1);
  lcd.print("               ");
  Serial.println(pad);
  lcd.setCursor(0, 1);
  lcd.print(pad);

  }
}
*/

